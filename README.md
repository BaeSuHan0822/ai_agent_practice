# My Own Agent

Minimal Python AI agent project using Poetry + LangChain + LangGraph + LangSmith.

## 1) Install dependencies

```bash
poetry install
```

## 2) Configure environment

```bash
cp .env.example .env
```

Then set `OPENAI_API_KEY` in `.env`.

## 3) Run

```bash
poetry run python main.py
```
